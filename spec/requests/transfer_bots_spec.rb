require 'rails_helper'

RSpec.describe "TransferBots", type: :request do
  describe "GET /transfer_bots" do
    xit "works! (now write some real specs)" do
      get transfer_bots_path
      expect(response).to have_http_status(200)
    end
  end
end
